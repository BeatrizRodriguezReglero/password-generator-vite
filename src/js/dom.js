// Aquí van los getElementById y los querySelectorAll que pondrías al principio.
const inputPasswordElement = document.getElementById('password');

const lengthRangePasswordElement = document.getElementById('length-range');

const inputRangeElement = document.getElementById('range');

const buttonPasswordElement = document.getElementById('button-password');

export {
	inputPasswordElement,
	lengthRangePasswordElement,
	inputRangeElement,
	buttonPasswordElement
};
